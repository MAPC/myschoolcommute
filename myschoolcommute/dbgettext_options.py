from dbgettext.registry import registry, Options
from dbgettext.lexicons import html    
from survey.models import School

#class SchoolOptions(Options):
#    attributes = ('survey_incentive',)
    # parsed_attributes = {'body': html.lexicon}
    
#registry.register(School, SchoolOptions)
